info:
  name: confirmWordOfTheDay
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/confirmWordOfTheDay"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
