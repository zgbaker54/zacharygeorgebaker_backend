info:
  name: confirmWordOfTheDay
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/getWordOfTheDay"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​