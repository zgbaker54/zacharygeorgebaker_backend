info:
  name: annotate7LettersGuessSequence
  type: http

http:
  method: POST
  url: "{{baseUrl}}/annotate7LettersGuessSequence"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​