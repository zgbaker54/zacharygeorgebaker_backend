info:
  name: getWordOfTheDay
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/getWordOfTheDay"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
