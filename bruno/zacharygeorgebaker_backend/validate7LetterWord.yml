info:
  name: validate7LetterWord
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/validate7LetterWord?word=rainbow"
  params:
    - name: word
      value: rainbow
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
